To run LRU:
compile and run LRU.java
enter the number of frames
the program will then prompt for the input of an integer for the page
it will then display the state of the frames
after 10 iterations it will also display the page faults